# Не нужно для "скачал-и-запустил" - отдельная ветка работы/большая БД
module_generative/
cancer_data.db
__pycache__/
*.pyc
*.log

# DiffSBDD - код+чекпоинт НУЖНЫ (генерация, Test B), но это склонированный
# внешний репозиторий со своим .git - только его саму метадату убираем,
# не содержимое
DiffSBDD/.git/

# Регенерируемые временные/промежуточные файлы докинга - НЕ нужны для
# запуска (main очередь их не читает), можно исключить ради размера
runs/*/dock_tmp/
runs/redock_*/
runs/crossdock_*/
runs/test_b_*/poses/
